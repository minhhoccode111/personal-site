const connect = require("./../config/dbConnect");

const User = require("./../models/User");
const Article = require("./../models/Article");
const Comment = require("./../models/Comment");

const main = async () => {
  // TODO: populate db here
};

connect(main);
